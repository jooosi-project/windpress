diff --git a/dist/core.mjs b/dist/core.mjs
index ccaf25a83c3db024e086fdd1b8927dd1bebe284e..ccb52b4d6789137b6b107e5c25144e5f4048efe0 100644
--- a/dist/core.mjs
+++ b/dist/core.mjs
@@ -287,9 +287,8 @@ function hydrate(options) {
   return lazy(options);
 }
 async function loadMonaco(highlighter, workspace, lsp) {
-  let cdnUrl = `https://esm.sh/modern-monaco@${version}`;
-  let editorCoreModuleUrl = `${cdnUrl}/es2022/editor-core.mjs`;
-  let lspModuleUrl = `${cdnUrl}/es2022/lsp.mjs`;
+  let editorCoreModuleUrl;
+  let lspModuleUrl;
   let importmapEl = null;
   if (importmapEl = document.querySelector("script[type='importmap']")) {
     try {
@@ -304,15 +303,19 @@ async function loadMonaco(highlighter, workspace, lsp) {
     }
   }
   const useBuiltinLSP = globalThis.MonacoEnvironment?.useBuiltinLSP;
+  const editorCoreModule = editorCoreModuleUrl ? import(
+    /* @vite-ignore */
+    /* webpackIgnore: true */
+    editorCoreModuleUrl
+  ) : import("./editor-core.mjs");
+  const builtinLSPModule = useBuiltinLSP ? lspModuleUrl ? import(
+    /* @vite-ignore */
+    /* webpackIgnore: true */
+    lspModuleUrl
+  ) : import("./lsp/index.mjs") : Promise.resolve({ builtinLSPProviders: {} });
   const [monaco, { builtinLSPProviders }] = await Promise.all([
-    import(
-      /* webpackIgnore: true */
-      editorCoreModuleUrl
-    ),
-    useBuiltinLSP ? import(
-      /* webpackIgnore: true */
-      lspModuleUrl
-    ) : Promise.resolve({ builtinLSPProviders: {} })
+    editorCoreModule,
+    builtinLSPModule
   ]);
   const allLspProviders = { ...builtinLSPProviders, ...lspProviders, ...lsp?.providers };
   workspace?.setupMonaco(monaco);
diff --git a/dist/editor-core.mjs b/dist/editor-core.mjs
--- a/dist/editor-core.mjs
+++ b/dist/editor-core.mjs
@@ -200742,14 +200742,14 @@ function convertPickItem(item) {
   return item;
 }
 function createEditorWorkerMain() {
-  const workerUrl = new URL("./editor-worker-main.mjs", import.meta.url);
-  if (workerUrl.origin !== location.origin) {
-    return new Worker(
-      URL.createObjectURL(new Blob([`import "${workerUrl.href}"`], { type: "application/javascript" })),
-      { type: "module", name: "editor-worker-main" }
-    );
-  }
-  return new Worker(workerUrl, { type: "module", name: "editor-worker-main" });
+  const workerUrl = new URL("./editor-worker-main.mjs", import.meta.url);
+  if (workerUrl.protocol !== "data:" && workerUrl.origin !== location.origin) {
+    return new Worker(
+      URL.createObjectURL(new Blob([`import "${workerUrl.href}"`], { type: "application/javascript" })),
+      { type: "module", name: "editor-worker-main" }
+    );
+  }
+  return new Worker(new URL("./editor-worker-main.mjs", import.meta.url), { type: "module", name: "editor-worker-main" });
 }
 export {
   CancellationTokenSource2 as CancellationTokenSource,
diff --git a/dist/lsp/css/setup.mjs b/dist/lsp/css/setup.mjs
--- a/dist/lsp/css/setup.mjs
+++ b/dist/lsp/css/setup.mjs
@@ -39,14 +39,14 @@ async function setup(monaco, languageId, languageSettings, formattingOptions, wo
   client.registerDocumentLinks(languageId, worker);
 }
 function createWebWorker() {
-  const workerUrl = new URL("./worker.mjs", import.meta.url);
-  if (workerUrl.origin !== location.origin) {
-    return new Worker(
-      URL.createObjectURL(new Blob([`import "${workerUrl.href}"`], { type: "application/javascript" })),
-      { type: "module", name: "css-worker" }
-    );
-  }
-  return new Worker(workerUrl, { type: "module", name: "css-worker" });
+  const workerUrl = new URL("./worker.mjs", import.meta.url);
+  if (workerUrl.protocol !== "data:" && workerUrl.origin !== location.origin) {
+    return new Worker(
+      URL.createObjectURL(new Blob([`import "${workerUrl.href}"`], { type: "application/javascript" })),
+      { type: "module", name: "css-worker" }
+    );
+  }
+  return new Worker(new URL("./worker.mjs", import.meta.url), { type: "module", name: "css-worker" });
 }
 function getWorker(createData) {
   const worker = createWebWorker();
diff --git a/dist/lsp/html/setup.mjs b/dist/lsp/html/setup.mjs
--- a/dist/lsp/html/setup.mjs
+++ b/dist/lsp/html/setup.mjs
@@ -91,14 +91,14 @@ async function setup(monaco, languageId, languageSettings, formattingOptions, wo
   }
 }
 function createWebWorker() {
-  const workerUrl = new URL("./worker.mjs", import.meta.url);
-  if (workerUrl.origin !== location.origin) {
-    return new Worker(
-      URL.createObjectURL(new Blob([`import "${workerUrl.href}"`], { type: "application/javascript" })),
-      { type: "module", name: "html-worker" }
-    );
-  }
-  return new Worker(workerUrl, { type: "module", name: "html-worker" });
+  const workerUrl = new URL("./worker.mjs", import.meta.url);
+  if (workerUrl.protocol !== "data:" && workerUrl.origin !== location.origin) {
+    return new Worker(
+      URL.createObjectURL(new Blob([`import "${workerUrl.href}"`], { type: "application/javascript" })),
+      { type: "module", name: "html-worker" }
+    );
+  }
+  return new Worker(new URL("./worker.mjs", import.meta.url), { type: "module", name: "html-worker" });
 }
 function getWorker(createData) {
   const worker = createWebWorker();
diff --git a/dist/lsp/json/setup.mjs b/dist/lsp/json/setup.mjs
--- a/dist/lsp/json/setup.mjs
+++ b/dist/lsp/json/setup.mjs
@@ -2721,14 +2721,14 @@ function toCompletionList(items) {
   return items;
 }
 function createWebWorker() {
-  const workerUrl = new URL("./worker.mjs", import.meta.url);
-  if (workerUrl.origin !== location.origin) {
-    return new Worker(
-      URL.createObjectURL(new Blob([`import "${workerUrl.href}"`], { type: "application/javascript" })),
-      { type: "module", name: "json-worker" }
-    );
-  }
-  return new Worker(workerUrl, { type: "module", name: "json-worker" });
+  const workerUrl = new URL("./worker.mjs", import.meta.url);
+  if (workerUrl.protocol !== "data:" && workerUrl.origin !== location.origin) {
+    return new Worker(
+      URL.createObjectURL(new Blob([`import "${workerUrl.href}"`], { type: "application/javascript" })),
+      { type: "module", name: "json-worker" }
+    );
+  }
+  return new Worker(new URL("./worker.mjs", import.meta.url), { type: "module", name: "json-worker" });
 }
 function getWorker(createData) {
   const worker = createWebWorker();
diff --git a/dist/lsp/typescript/setup.mjs b/dist/lsp/typescript/setup.mjs
--- a/dist/lsp/typescript/setup.mjs
+++ b/dist/lsp/typescript/setup.mjs
@@ -2689,14 +2689,14 @@ function getWorker(createData) {
   return worker2;
 }
 function createWebWorker() {
-  const workerUrl = new URL("./worker.mjs", import.meta.url);
-  if (workerUrl.origin !== location.origin) {
-    return new Worker(
-      URL.createObjectURL(new Blob([`import "${workerUrl.href}"`], { type: "application/javascript" })),
-      { type: "module", name: "typescript-worker" }
-    );
-  }
-  return new Worker(workerUrl, { type: "module", name: "typescript-worker" });
+  const workerUrl = new URL("./worker.mjs", import.meta.url);
+  if (workerUrl.protocol !== "data:" && workerUrl.origin !== location.origin) {
+    return new Worker(
+      URL.createObjectURL(new Blob([`import "${workerUrl.href}"`], { type: "application/javascript" })),
+      { type: "module", name: "typescript-worker" }
+    );
+  }
+  return new Worker(new URL("./worker.mjs", import.meta.url), { type: "module", name: "typescript-worker" });
 }
 function getWorker(createData) {
   const worker2 = createWebWorker();
